#ifndef COLOR_DEFINITIONS_H
#define COLOR_DEFINITIONS_H

#pragma once

#include <cstdint>

namespace aura3d {
namespace colors {

// Color tuple structure
struct RGB {
    uint8_t r;
    uint8_t g;
    uint8_t b;
    
    // Constructor for easy initialization
    RGB(uint8_t red, uint8_t green, uint8_t blue) : r(red), g(green), b(blue) {}
    
    // Convert to uint32_t (ARGB format)
    uint32_t toUint32() const {
        return 0xFF000000 | (r << 16) | (g << 8) | b;
    }
};

// Helper function to convert RGB to uint32_t (ARGB format with full alpha)
constexpr uint32_t RGB_TO_UINT32(uint8_t r, uint8_t g, uint8_t b) {
    return 0xFF000000 | (r << 16) | (g << 8) | b;
}

// ===============================================
// Basic Colors
// ===============================================

// Primary Colors
const RGB RED(255, 0, 0);
const uint32_t RED_UINT32 = RGB_TO_UINT32(255, 0, 0);

const RGB GREEN(0, 255, 0);
const uint32_t GREEN_UINT32 = RGB_TO_UINT32(0, 255, 0);

const RGB BLUE(0, 0, 255);
const uint32_t BLUE_UINT32 = RGB_TO_UINT32(0, 0, 255);

// Secondary Colors
const RGB YELLOW(255, 255, 0);
const uint32_t YELLOW_UINT32 = RGB_TO_UINT32(255, 255, 0);

const RGB CYAN(0, 255, 255);
const uint32_t CYAN_UINT32 = RGB_TO_UINT32(0, 255, 255);

const RGB MAGENTA(255, 0, 255);
const uint32_t MAGENTA_UINT32 = RGB_TO_UINT32(255, 0, 255);

// Grayscale
const RGB BLACK(0, 0, 0);
const uint32_t BLACK_UINT32 = RGB_TO_UINT32(0, 0, 0);

const RGB WHITE(255, 255, 255);
const uint32_t WHITE_UINT32 = RGB_TO_UINT32(255, 255, 255);

const RGB GRAY(128, 128, 128);
const uint32_t GRAY_UINT32 = RGB_TO_UINT32(128, 128, 128);

const RGB LIGHT_GRAY(192, 192, 192);
const uint32_t LIGHT_GRAY_UINT32 = RGB_TO_UINT32(192, 192, 192);

const RGB DARK_GRAY(64, 64, 64);
const uint32_t DARK_GRAY_UINT32 = RGB_TO_UINT32(64, 64, 64);

// ===============================================
// Web Standard Colors
// ===============================================

const RGB ALICE_BLUE(240, 248, 255);
const uint32_t ALICE_BLUE_UINT32 = RGB_TO_UINT32(240, 248, 255);

const RGB ANTIQUE_WHITE(250, 235, 215);
const uint32_t ANTIQUE_WHITE_UINT32 = RGB_TO_UINT32(250, 235, 215);

const RGB AQUA(0, 255, 255);
const uint32_t AQUA_UINT32 = RGB_TO_UINT32(0, 255, 255);

const RGB AQUAMARINE(127, 255, 212);
const uint32_t AQUAMARINE_UINT32 = RGB_TO_UINT32(127, 255, 212);

const RGB AZURE(240, 255, 255);
const uint32_t AZURE_UINT32 = RGB_TO_UINT32(240, 255, 255);

const RGB BEIGE(245, 245, 220);
const uint32_t BEIGE_UINT32 = RGB_TO_UINT32(245, 245, 220);

const RGB BISQUE(255, 228, 196);
const uint32_t BISQUE_UINT32 = RGB_TO_UINT32(255, 228, 196);

const RGB BLANCHED_ALMOND(255, 235, 205);
const uint32_t BLANCHED_ALMOND_UINT32 = RGB_TO_UINT32(255, 235, 205);

const RGB BLUE_VIOLET(138, 43, 226);
const uint32_t BLUE_VIOLET_UINT32 = RGB_TO_UINT32(138, 43, 226);

const RGB BROWN(165, 42, 42);
const uint32_t BROWN_UINT32 = RGB_TO_UINT32(165, 42, 42);

const RGB BURLYWOOD(222, 184, 135);
const uint32_t BURLYWOOD_UINT32 = RGB_TO_UINT32(222, 184, 135);

const RGB CADET_BLUE(95, 158, 160);
const uint32_t CADET_BLUE_UINT32 = RGB_TO_UINT32(95, 158, 160);

const RGB CHARTREUSE(127, 255, 0);
const uint32_t CHARTREUSE_UINT32 = RGB_TO_UINT32(127, 255, 0);

const RGB CHOCOLATE(210, 105, 30);
const uint32_t CHOCOLATE_UINT32 = RGB_TO_UINT32(210, 105, 30);

const RGB CORAL(255, 127, 80);
const uint32_t CORAL_UINT32 = RGB_TO_UINT32(255, 127, 80);

const RGB CORNFLOWER_BLUE(100, 149, 237);
const uint32_t CORNFLOWER_BLUE_UINT32 = RGB_TO_UINT32(100, 149, 237);

const RGB CORNSILK(255, 248, 220);
const uint32_t CORNSILK_UINT32 = RGB_TO_UINT32(255, 248, 220);

const RGB CRIMSON(220, 20, 60);
const uint32_t CRIMSON_UINT32 = RGB_TO_UINT32(220, 20, 60);

const RGB DARK_BLUE(0, 0, 139);
const uint32_t DARK_BLUE_UINT32 = RGB_TO_UINT32(0, 0, 139);

const RGB DARK_CYAN(0, 139, 139);
const uint32_t DARK_CYAN_UINT32 = RGB_TO_UINT32(0, 139, 139);

const RGB DARK_GOLDENROD(184, 134, 11);
const uint32_t DARK_GOLDENROD_UINT32 = RGB_TO_UINT32(184, 134, 11);

const RGB DARK_GREEN(0, 100, 0);
const uint32_t DARK_GREEN_UINT32 = RGB_TO_UINT32(0, 100, 0);

const RGB DARK_KHAKI(189, 183, 107);
const uint32_t DARK_KHAKI_UINT32 = RGB_TO_UINT32(189, 183, 107);

const RGB DARK_MAGENTA(139, 0, 139);
const uint32_t DARK_MAGENTA_UINT32 = RGB_TO_UINT32(139, 0, 139);

const RGB DARK_OLIVE_GREEN(85, 107, 47);
const uint32_t DARK_OLIVE_GREEN_UINT32 = RGB_TO_UINT32(85, 107, 47);

const RGB DARK_ORANGE(255, 140, 0);
const uint32_t DARK_ORANGE_UINT32 = RGB_TO_UINT32(255, 140, 0);

const RGB DARK_ORCHID(153, 50, 204);
const uint32_t DARK_ORCHID_UINT32 = RGB_TO_UINT32(153, 50, 204);

const RGB DARK_RED(139, 0, 0);
const uint32_t DARK_RED_UINT32 = RGB_TO_UINT32(139, 0, 0);

const RGB DARK_SALMON(233, 150, 122);
const uint32_t DARK_SALMON_UINT32 = RGB_TO_UINT32(233, 150, 122);

const RGB DARK_SEA_GREEN(143, 188, 143);
const uint32_t DARK_SEA_GREEN_UINT32 = RGB_TO_UINT32(143, 188, 143);

const RGB DARK_SLATE_BLUE(72, 61, 139);
const uint32_t DARK_SLATE_BLUE_UINT32 = RGB_TO_UINT32(72, 61, 139);

const RGB DARK_SLATE_GRAY(47, 79, 79);
const uint32_t DARK_SLATE_GRAY_UINT32 = RGB_TO_UINT32(47, 79, 79);

const RGB DARK_TURQUOISE(0, 206, 209);
const uint32_t DARK_TURQUOISE_UINT32 = RGB_TO_UINT32(0, 206, 209);

const RGB DARK_VIOLET(148, 0, 211);
const uint32_t DARK_VIOLET_UINT32 = RGB_TO_UINT32(148, 0, 211);

const RGB DEEP_PINK(255, 20, 147);
const uint32_t DEEP_PINK_UINT32 = RGB_TO_UINT32(255, 20, 147);

const RGB DEEP_SKY_BLUE(0, 191, 255);
const uint32_t DEEP_SKY_BLUE_UINT32 = RGB_TO_UINT32(0, 191, 255);

const RGB DIM_GRAY(105, 105, 105);
const uint32_t DIM_GRAY_UINT32 = RGB_TO_UINT32(105, 105, 105);

const RGB DODGER_BLUE(30, 144, 255);
const uint32_t DODGER_BLUE_UINT32 = RGB_TO_UINT32(30, 144, 255);

const RGB FIREBRICK(178, 34, 34);
const uint32_t FIREBRICK_UINT32 = RGB_TO_UINT32(178, 34, 34);

const RGB FLORAL_WHITE(255, 250, 240);
const uint32_t FLORAL_WHITE_UINT32 = RGB_TO_UINT32(255, 250, 240);

const RGB FOREST_GREEN(34, 139, 34);
const uint32_t FOREST_GREEN_UINT32 = RGB_TO_UINT32(34, 139, 34);

const RGB FUCHSIA(255, 0, 255);
const uint32_t FUCHSIA_UINT32 = RGB_TO_UINT32(255, 0, 255);

const RGB GAINSBORO(220, 220, 220);
const uint32_t GAINSBORO_UINT32 = RGB_TO_UINT32(220, 220, 220);

const RGB GHOST_WHITE(248, 248, 255);
const uint32_t GHOST_WHITE_UINT32 = RGB_TO_UINT32(248, 248, 255);

const RGB GOLD(255, 215, 0);
const uint32_t GOLD_UINT32 = RGB_TO_UINT32(255, 215, 0);

const RGB GOLDENROD(218, 165, 32);
const uint32_t GOLDENROD_UINT32 = RGB_TO_UINT32(218, 165, 32);

const RGB GRAY_WEB(128, 128, 128);
const uint32_t GRAY_WEB_UINT32 = RGB_TO_UINT32(128, 128, 128);

const RGB GREEN_WEB(0, 128, 0);
const uint32_t GREEN_WEB_UINT32 = RGB_TO_UINT32(0, 128, 0);

const RGB GREEN_YELLOW(173, 255, 47);
const uint32_t GREEN_YELLOW_UINT32 = RGB_TO_UINT32(173, 255, 47);

const RGB HONEYDEW(240, 255, 240);
const uint32_t HONEYDEW_UINT32 = RGB_TO_UINT32(240, 255, 240);

const RGB HOT_PINK(255, 105, 180);
const uint32_t HOT_PINK_UINT32 = RGB_TO_UINT32(255, 105, 180);

const RGB INDIAN_RED(205, 92, 92);
const uint32_t INDIAN_RED_UINT32 = RGB_TO_UINT32(205, 92, 92);

const RGB INDIGO(75, 0, 130);
const uint32_t INDIGO_UINT32 = RGB_TO_UINT32(75, 0, 130);

const RGB IVORY(255, 255, 240);
const uint32_t IVORY_UINT32 = RGB_TO_UINT32(255, 255, 240);

const RGB KHAKI(240, 230, 140);
const uint32_t KHAKI_UINT32 = RGB_TO_UINT32(240, 230, 140);

const RGB LAVENDER(230, 230, 250);
const uint32_t LAVENDER_UINT32 = RGB_TO_UINT32(230, 230, 250);

const RGB LAVENDER_BLUSH(255, 240, 245);
const uint32_t LAVENDER_BLUSH_UINT32 = RGB_TO_UINT32(255, 240, 245);

const RGB LAWN_GREEN(124, 252, 0);
const uint32_t LAWN_GREEN_UINT32 = RGB_TO_UINT32(124, 252, 0);

const RGB LEMON_CHIFFON(255, 250, 205);
const uint32_t LEMON_CHIFFON_UINT32 = RGB_TO_UINT32(255, 250, 205);

const RGB LIGHT_BLUE(173, 216, 230);
const uint32_t LIGHT_BLUE_UINT32 = RGB_TO_UINT32(173, 216, 230);

const RGB LIGHT_CORAL(240, 128, 128);
const uint32_t LIGHT_CORAL_UINT32 = RGB_TO_UINT32(240, 128, 128);

const RGB LIGHT_CYAN(224, 255, 255);
const uint32_t LIGHT_CYAN_UINT32 = RGB_TO_UINT32(224, 255, 255);

const RGB LIGHT_GOLDENROD_YELLOW(250, 250, 210);
const uint32_t LIGHT_GOLDENROD_YELLOW_UINT32 = RGB_TO_UINT32(250, 250, 210);

const RGB LIGHT_GREEN(144, 238, 144);
const uint32_t LIGHT_GREEN_UINT32 = RGB_TO_UINT32(144, 238, 144);

const RGB LIGHT_PINK(255, 182, 193);
const uint32_t LIGHT_PINK_UINT32 = RGB_TO_UINT32(255, 182, 193);

const RGB LIGHT_SALMON(255, 160, 122);
const uint32_t LIGHT_SALMON_UINT32 = RGB_TO_UINT32(255, 160, 122);

const RGB LIGHT_SEA_GREEN(32, 178, 170);
const uint32_t LIGHT_SEA_GREEN_UINT32 = RGB_TO_UINT32(32, 178, 170);

const RGB LIGHT_SKY_BLUE(135, 206, 250);
const uint32_t LIGHT_SKY_BLUE_UINT32 = RGB_TO_UINT32(135, 206, 250);

const RGB LIGHT_SLATE_GRAY(119, 136, 153);
const uint32_t LIGHT_SLATE_GRAY_UINT32 = RGB_TO_UINT32(119, 136, 153);

const RGB LIGHT_STEEL_BLUE(176, 196, 222);
const uint32_t LIGHT_STEEL_BLUE_UINT32 = RGB_TO_UINT32(176, 196, 222);

const RGB LIGHT_YELLOW(255, 255, 224);
const uint32_t LIGHT_YELLOW_UINT32 = RGB_TO_UINT32(255, 255, 224);

const RGB LIME(0, 255, 0);
const uint32_t LIME_UINT32 = RGB_TO_UINT32(0, 255, 0);

const RGB LIME_GREEN(50, 205, 50);
const uint32_t LIME_GREEN_UINT32 = RGB_TO_UINT32(50, 205, 50);

const RGB LINEN(250, 240, 230);
const uint32_t LINEN_UINT32 = RGB_TO_UINT32(250, 240, 230);

const RGB MAROON(128, 0, 0);
const uint32_t MAROON_UINT32 = RGB_TO_UINT32(128, 0, 0);

const RGB MEDIUM_AQUAMARINE(102, 205, 170);
const uint32_t MEDIUM_AQUAMARINE_UINT32 = RGB_TO_UINT32(102, 205, 170);

const RGB MEDIUM_BLUE(0, 0, 205);
const uint32_t MEDIUM_BLUE_UINT32 = RGB_TO_UINT32(0, 0, 205);

const RGB MEDIUM_ORCHID(186, 85, 211);
const uint32_t MEDIUM_ORCHID_UINT32 = RGB_TO_UINT32(186, 85, 211);

const RGB MEDIUM_PURPLE(147, 112, 219);
const uint32_t MEDIUM_PURPLE_UINT32 = RGB_TO_UINT32(147, 112, 219);

const RGB MEDIUM_SEA_GREEN(60, 179, 113);
const uint32_t MEDIUM_SEA_GREEN_UINT32 = RGB_TO_UINT32(60, 179, 113);

const RGB MEDIUM_SLATE_BLUE(123, 104, 238);
const uint32_t MEDIUM_SLATE_BLUE_UINT32 = RGB_TO_UINT32(123, 104, 238);

const RGB MEDIUM_SPRING_GREEN(0, 250, 154);
const uint32_t MEDIUM_SPRING_GREEN_UINT32 = RGB_TO_UINT32(0, 250, 154);

const RGB MEDIUM_TURQUOISE(72, 209, 204);
const uint32_t MEDIUM_TURQUOISE_UINT32 = RGB_TO_UINT32(72, 209, 204);

const RGB MEDIUM_VIOLET_RED(199, 21, 133);
const uint32_t MEDIUM_VIOLET_RED_UINT32 = RGB_TO_UINT32(199, 21, 133);

const RGB MIDNIGHT_BLUE(25, 25, 112);
const uint32_t MIDNIGHT_BLUE_UINT32 = RGB_TO_UINT32(25, 25, 112);

const RGB MINT_CREAM(245, 255, 250);
const uint32_t MINT_CREAM_UINT32 = RGB_TO_UINT32(245, 255, 250);

const RGB MISTY_ROSE(255, 228, 225);
const uint32_t MISTY_ROSE_UINT32 = RGB_TO_UINT32(255, 228, 225);

const RGB MOCCASIN(255, 228, 181);
const uint32_t MOCCASIN_UINT32 = RGB_TO_UINT32(255, 228, 181);

const RGB NAVAJO_WHITE(255, 222, 173);
const uint32_t NAVAJO_WHITE_UINT32 = RGB_TO_UINT32(255, 222, 173);

const RGB NAVY(0, 0, 128);
const uint32_t NAVY_UINT32 = RGB_TO_UINT32(0, 0, 128);

const RGB OLD_LACE(253, 245, 230);
const uint32_t OLD_LACE_UINT32 = RGB_TO_UINT32(253, 245, 230);

const RGB OLIVE(128, 128, 0);
const uint32_t OLIVE_UINT32 = RGB_TO_UINT32(128, 128, 0);

const RGB OLIVE_DRAB(107, 142, 35);
const uint32_t OLIVE_DRAB_UINT32 = RGB_TO_UINT32(107, 142, 35);

const RGB ORANGE(255, 165, 0);
const uint32_t ORANGE_UINT32 = RGB_TO_UINT32(255, 165, 0);

const RGB ORANGE_RED(255, 69, 0);
const uint32_t ORANGE_RED_UINT32 = RGB_TO_UINT32(255, 69, 0);

const RGB ORCHID(218, 112, 214);
const uint32_t ORCHID_UINT32 = RGB_TO_UINT32(218, 112, 214);

const RGB PALE_GOLDENROD(238, 232, 170);
const uint32_t PALE_GOLDENROD_UINT32 = RGB_TO_UINT32(238, 232, 170);

const RGB PALE_GREEN(152, 251, 152);
const uint32_t PALE_GREEN_UINT32 = RGB_TO_UINT32(152, 251, 152);

const RGB PALE_TURQUOISE(175, 238, 238);
const uint32_t PALE_TURQUOISE_UINT32 = RGB_TO_UINT32(175, 238, 238);

const RGB PALE_VIOLET_RED(219, 112, 147);
const uint32_t PALE_VIOLET_RED_UINT32 = RGB_TO_UINT32(219, 112, 147);

const RGB PAPAYA_WHIP(255, 239, 213);
const uint32_t PAPAYA_WHIP_UINT32 = RGB_TO_UINT32(255, 239, 213);

const RGB PEACH_PUFF(255, 218, 185);
const uint32_t PEACH_PUFF_UINT32 = RGB_TO_UINT32(255, 218, 185);

const RGB PERU(205, 133, 63);
const uint32_t PERU_UINT32 = RGB_TO_UINT32(205, 133, 63);

const RGB PINK(255, 192, 203);
const uint32_t PINK_UINT32 = RGB_TO_UINT32(255, 192, 203);

const RGB PLUM(221, 160, 221);
const uint32_t PLUM_UINT32 = RGB_TO_UINT32(221, 160, 221);

const RGB POWDER_BLUE(176, 224, 230);
const uint32_t POWDER_BLUE_UINT32 = RGB_TO_UINT32(176, 224, 230);

const RGB PURPLE(128, 0, 128);
const uint32_t PURPLE_UINT32 = RGB_TO_UINT32(128, 0, 128);

const RGB REBECCA_PURPLE(102, 51, 153);
const uint32_t REBECCA_PURPLE_UINT32 = RGB_TO_UINT32(102, 51, 153);

const RGB ROSY_BROWN(188, 143, 143);
const uint32_t ROSY_BROWN_UINT32 = RGB_TO_UINT32(188, 143, 143);

const RGB ROYAL_BLUE(65, 105, 225);
const uint32_t ROYAL_BLUE_UINT32 = RGB_TO_UINT32(65, 105, 225);

const RGB SADDLE_BROWN(139, 69, 19);
const uint32_t SADDLE_BROWN_UINT32 = RGB_TO_UINT32(139, 69, 19);

const RGB SALMON(250, 128, 114);
const uint32_t SALMON_UINT32 = RGB_TO_UINT32(250, 128, 114);

const RGB SANDY_BROWN(244, 164, 96);
const uint32_t SANDY_BROWN_UINT32 = RGB_TO_UINT32(244, 164, 96);

const RGB SEA_GREEN(46, 139, 87);
const uint32_t SEA_GREEN_UINT32 = RGB_TO_UINT32(46, 139, 87);

const RGB SEASHELL(255, 245, 238);
const uint32_t SEASHELL_UINT32 = RGB_TO_UINT32(255, 245, 238);

const RGB SIENNA(160, 82, 45);
const uint32_t SIENNA_UINT32 = RGB_TO_UINT32(160, 82, 45);

const RGB SILVER(192, 192, 192);
const uint32_t SILVER_UINT32 = RGB_TO_UINT32(192, 192, 192);

const RGB SKY_BLUE(135, 206, 235);
const uint32_t SKY_BLUE_UINT32 = RGB_TO_UINT32(135, 206, 235);

const RGB SLATE_BLUE(106, 90, 205);
const uint32_t SLATE_BLUE_UINT32 = RGB_TO_UINT32(106, 90, 205);

const RGB SLATE_GRAY(112, 128, 144);
const uint32_t SLATE_GRAY_UINT32 = RGB_TO_UINT32(112, 128, 144);

const RGB SNOW(255, 250, 250);
const uint32_t SNOW_UINT32 = RGB_TO_UINT32(255, 250, 250);

const RGB SPRING_GREEN(0, 255, 127);
const uint32_t SPRING_GREEN_UINT32 = RGB_TO_UINT32(0, 255, 127);

const RGB STEEL_BLUE(70, 130, 180);
const uint32_t STEEL_BLUE_UINT32 = RGB_TO_UINT32(70, 130, 180);

const RGB TAN(210, 180, 140);
const uint32_t TAN_UINT32 = RGB_TO_UINT32(210, 180, 140);

const RGB TEAL(0, 128, 128);
const uint32_t TEAL_UINT32 = RGB_TO_UINT32(0, 128, 128);

const RGB THISTLE(216, 191, 216);
const uint32_t THISTLE_UINT32 = RGB_TO_UINT32(216, 191, 216);

const RGB TOMATO(255, 99, 71);
const uint32_t TOMATO_UINT32 = RGB_TO_UINT32(255, 99, 71);

const RGB TURQUOISE(64, 224, 208);
const uint32_t TURQUOISE_UINT32 = RGB_TO_UINT32(64, 224, 208);

const RGB VIOLET(238, 130, 238);
const uint32_t VIOLET_UINT32 = RGB_TO_UINT32(238, 130, 238);

const RGB WHEAT(245, 222, 179);
const uint32_t WHEAT_UINT32 = RGB_TO_UINT32(245, 222, 179);

const RGB WHITE_SMOKE(245, 245, 245);
const uint32_t WHITE_SMOKE_UINT32 = RGB_TO_UINT32(245, 245, 245);

const RGB YELLOW_GREEN(154, 205, 50);
const uint32_t YELLOW_GREEN_UINT32 = RGB_TO_UINT32(154, 205, 50);

// ===============================================
// Material Design Colors
// ===============================================

const RGB MAT_RED_500(244, 67, 54);
const uint32_t MAT_RED_500_UINT32 = RGB_TO_UINT32(244, 67, 54);

const RGB MAT_PINK_500(233, 30, 99);
const uint32_t MAT_PINK_500_UINT32 = RGB_TO_UINT32(233, 30, 99);

const RGB MAT_PURPLE_500(156, 39, 176);
const uint32_t MAT_PURPLE_500_UINT32 = RGB_TO_UINT32(156, 39, 176);

const RGB MAT_DEEP_PURPLE_500(103, 58, 183);
const uint32_t MAT_DEEP_PURPLE_500_UINT32 = RGB_TO_UINT32(103, 58, 183);

const RGB MAT_INDIGO_500(63, 81, 181);
const uint32_t MAT_INDIGO_500_UINT32 = RGB_TO_UINT32(63, 81, 181);

const RGB MAT_BLUE_500(33, 150, 243);
const uint32_t MAT_BLUE_500_UINT32 = RGB_TO_UINT32(33, 150, 243);

const RGB MAT_LIGHT_BLUE_500(3, 169, 244);
const uint32_t MAT_LIGHT_BLUE_500_UINT32 = RGB_TO_UINT32(3, 169, 244);

const RGB MAT_CYAN_500(0, 188, 212);
const uint32_t MAT_CYAN_500_UINT32 = RGB_TO_UINT32(0, 188, 212);

const RGB MAT_TEAL_500(0, 150, 136);
const uint32_t MAT_TEAL_500_UINT32 = RGB_TO_UINT32(0, 150, 136);

const RGB MAT_GREEN_500(76, 175, 80);
const uint32_t MAT_GREEN_500_UINT32 = RGB_TO_UINT32(76, 175, 80);

const RGB MAT_LIGHT_GREEN_500(139, 195, 74);
const uint32_t MAT_LIGHT_GREEN_500_UINT32 = RGB_TO_UINT32(139, 195, 74);

const RGB MAT_LIME_500(205, 220, 57);
const uint32_t MAT_LIME_500_UINT32 = RGB_TO_UINT32(205, 220, 57);

const RGB MAT_YELLOW_500(255, 235, 59);
const uint32_t MAT_YELLOW_500_UINT32 = RGB_TO_UINT32(255, 235, 59);

const RGB MAT_AMBER_500(255, 193, 7);
const uint32_t MAT_AMBER_500_UINT32 = RGB_TO_UINT32(255, 193, 7);

const RGB MAT_ORANGE_500(255, 152, 0);
const uint32_t MAT_ORANGE_500_UINT32 = RGB_TO_UINT32(255, 152, 0);

const RGB MAT_DEEP_ORANGE_500(255, 87, 34);
const uint32_t MAT_DEEP_ORANGE_500_UINT32 = RGB_TO_UINT32(255, 87, 34);

const RGB MAT_BROWN_500(121, 85, 72);
const uint32_t MAT_BROWN_500_UINT32 = RGB_TO_UINT32(121, 85, 72);

const RGB MAT_GREY_500(158, 158, 158);
const uint32_t MAT_GREY_500_UINT32 = RGB_TO_UINT32(158, 158, 158);

const RGB MAT_BLUE_GREY_500(96, 125, 139);
const uint32_t MAT_BLUE_GREY_500_UINT32 = RGB_TO_UINT32(96, 125, 139);

} // namespace colors
} // namespace aura3d

#endif // COLOR_DEFINITIONS_H
