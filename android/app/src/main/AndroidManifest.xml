<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Vulkan requires no special permission, but we need INTERNET for future
         asset streaming and VIBRATE for optional haptic feedback. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Declare minimum Vulkan support requirement -->
    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:required="true"
        android:version="1" />
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="true"
        android:version="0x00401000" /> <!-- Vulkan 1.1 -->

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        <!--
            MainActivity (extends SDL3's org.libsdl.app.SDLActivity, copied in
            from SDL 3.4.10's android-project template; see
            android/app/src/main/java/org/libsdl/app/) bootstraps the native
            library (libmain.so): it handles the surface lifecycle, touch
            input, and calls SDL_main(). The override only exists because
            Aura3D links SDL3 statically into libmain.so rather than as a
            separate libSDL3.so; see MainActivity's own comment.
        -->
        <activity
            android:name="com.aura3d.sandbox.MainActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize"
            android:resizeableActivity="false"
            android:screenOrientation="landscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
